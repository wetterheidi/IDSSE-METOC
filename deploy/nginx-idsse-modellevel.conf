server {
    listen 80;
    server_name idsseml.wetterheidi.de;

    root /apps/IDSSE-METOC-modellevel;
    index index.html;

    auth_basic "IDSSE METOC";
    auth_basic_user_file /etc/nginx/.htpasswd-wetterheidi;

    location / {
        try_files $uri $uri/ =404;
    }
}
